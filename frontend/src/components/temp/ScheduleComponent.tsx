// Temporary component to fix build errors
interface ScheduleComponentProps {
  onSchedule: () => void;
}

export const ScheduleComponent = ({ onSchedule }: ScheduleComponentProps) => {
  return null;
};